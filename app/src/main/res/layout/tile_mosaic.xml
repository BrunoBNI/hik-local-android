<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="0dp"
    android:layout_height="wrap_content"
    android:layout_columnWeight="1"
    android:layout_margin="4dp"
    android:background="@color/panel"
    android:orientation="vertical">

    <FrameLayout
        android:id="@+id/tileFrame"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:background="@color/black">

        <ImageView
            android:id="@+id/tileImage"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:adjustViewBounds="false"
            android:contentDescription="@string/mosaic_preview_desc"
            android:scaleType="centerCrop" />

        <ProgressBar
            android:id="@+id/tileLoading"
            android:layout_width="26dp"
            android:layout_height="26dp"
            android:layout_gravity="center"
            android:indeterminateTint="@color/accent" />

        <TextView
            android:id="@+id/tileOff"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@color/bg"
            android:gravity="center"
            android:text="⏻"
            android:textColor="@color/text_dim"
            android:textSize="22sp"
            android:visibility="gone" />
    </FrameLayout>

    <!--
      Le ratio 16:9 de la vignette est fixé par code (post-mesure), GridLayout
      ne supportant pas app:layout_constraintDimensionRatio.
    -->

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:paddingStart="8dp"
        android:paddingEnd="4dp">

        <TextView
            android:id="@+id/tileLabel"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:ellipsize="end"
            android:maxLines="1"
            android:paddingTop="6dp"
            android:paddingBottom="6dp"
            android:textColor="@color/text_dim"
            android:textSize="12sp" />

        <ImageButton
            android:id="@+id/tileToggle"
            android:layout_width="36dp"
            android:layout_height="36dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/mosaic_toggle_desc"
            android:src="@android:drawable/ic_lock_power_off" />
    </LinearLayout>
</LinearLayout>
